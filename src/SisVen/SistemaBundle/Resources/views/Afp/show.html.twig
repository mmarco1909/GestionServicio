{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Afp</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Afp</th>
                <td>{{ entity.afp }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('afp') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('afp_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
