{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProcesoResultado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Resultado</th>
                <td>{{ entity.resultado }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ entity.activo }}</td>
            </tr>
            <tr>
                <th>Input</th>
                <td>{{ entity.input }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('procesoresultado') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
