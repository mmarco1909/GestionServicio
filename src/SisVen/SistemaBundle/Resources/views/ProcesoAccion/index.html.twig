{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProcesoAccion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Accion</th>
                <th>Activo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('procesoaccion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.accion }}</td>
                <td>{{ entity.activo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('procesoaccion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
