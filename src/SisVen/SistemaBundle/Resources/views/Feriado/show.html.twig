{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Feriado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Motivo</th>
                <td>{{ entity.motivo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('feriado') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
